import { PlatformNamePipe } from './platform-name.pipe';

describe('PlatformNamePipe', () => {
  it('create an instance', () => {
    const pipe = new PlatformNamePipe();
    expect(pipe).toBeTruthy();
  });
});
