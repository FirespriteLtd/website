import { GamesbyplatformPipe } from './gamesbyplatform.pipe';

describe('GamesbyplatformPipe', () => {
  it('create an instance', () => {
    const pipe = new GamesbyplatformPipe();
    expect(pipe).toBeTruthy();
  });
});
